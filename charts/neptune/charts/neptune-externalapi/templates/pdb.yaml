apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "externalapi.fullname" . }}-pdb
spec:
  maxUnavailable: 0
  selector:
    matchLabels:
      app: {{ include "externalapi.fullname" . }}
